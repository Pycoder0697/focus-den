# OS / editor cruft
.DS_Store

# Local Claude Code config (settings.local.json, launch.json — machine-specific)
.claude/

# Cloudflare Wrangler local state (can cache build artifacts)
**/.wrangler/

# Local secrets (also covered by per-folder .gitignores — belt & suspenders)
**/.env
**/.dev.vars
**/vapid-private.json
index.test.html
